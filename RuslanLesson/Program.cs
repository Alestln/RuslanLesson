namespace RuslanLesson
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
             * Целочисленные типы данных
             * 
             * signed types - знаковые типы
             * 
             * sbyte - 8-битное целое число со знаком
             * Занимает 1 байт памяти
             * Диапазон значений: от -128 до 127
             * 
             * short - 16-битное целое число со знаком
             * Занимает 2 байта памяти
             * Диапазон значений: от -32,768 до 32,767
             * 
             * int - 32-битное целое число со знаком
             * Занимает 4 байта памяти
             * Диапазон значений: от -2,147,483,648 до 2,147,483,647
             * 
             * long - 64-битное целое число со знаком
             * Занимает 8 байт памяти
             * Диапазон значений: от -9,223,372,036,854,775,808 до 9,223,372,036,854,775,807
             * 
             * unsigned types - беззнаковые типы
             * 
             * byte - 8-битное целое число без знака
             * Занимает 1 байт памяти
             * Диапазон значений: от 0 до 255
             * 
             * ushort - 16-битное целое число без знака
             * Занимает 2 байта памяти
             * Диапазон значений: от 0 до 65,535
             * 
             * uint - 32-битное целое число без знака
             * Занимает 4 байта памяти
             * Диапазон значений: от 0 до 4,294,967,295
             * 
             * ulong - 64-битное целое число без знака
             * Занимает 8 байт памяти
             * Диапазон значений: от 0 до 18,446,744,073,709,551,615
             * 
             * Типы с плавающей запятой
             * 
             * float - 32-битное число с плавающей запятой одинарной точности
             * Занимает 4 байта памяти
             * Точность: примерно 6-9 цифр
             * 
             * double - 64-битное число с плавающей запятой двойной точности
             * Занимает 8 байт памяти
             * Точность: примерно 15-17 цифр
             * 
             * decimal - 128-битное десятичное число с плавающей запятой, используемое для финансовых и денежных расчетов
             * Занимает 16 байт памяти
             * Точность: примерно 28-29 цифр
             * 
             * Символьные типы данных
             * 
             * char - 16-битный символ Unicode
             * Занимает 2 байта памяти
             * Использует кодировку UTF-16
             * 
             * Строковые типы данных
             * 
             * string - последовательность символов Unicode
             * Занимает 2 байта на каждый символ
             * 
             * Логические типы данных
             * 
             * bool - логический тип данных
             * Занимает 1 байт памяти
             * Хранит значения true или false
             */

            Console.Write("Введите свое имя: ");
            string name = Console.ReadLine();

            Console.Write("Введите свой возраст: ");
            // Парсинг - преобразование строки в значение другого типа данных
            // Старый способ парсинга
            // int age = Convert.ToInt32(Console.ReadLine());

            // Новый способ парсинга (рекомендуется использовать его)
            int age = int.Parse(Console.ReadLine());
            // double pi = double.Parse("3.14"); // Пример парсинга строки в число с плавающей запятой
            // bool result = bool.Parse("true"); // Пример парсинга строки в логическое значение

            // Привет, меня зовут Руслан и мне 17 лет.
            Console.WriteLine($"Привет, меня зовут {name} и мне {age} лет."); // Интерполяция строк
            // Console.WriteLine("Привет, меня зовут {0} и мне {1} лет.", name, age); // Форматированный вывод
            // Console.WriteLine("Привет, меня зовут " + name + " и мне " + age + " лет."); // Конкатенация строк

            // Домашнее задание
            /*
             * Создать анкету на прием участия в киберспортивной команде
             * Указать в анкете следующие данные:
             * 1. Название команды
             * 2. Игровой никнейм
             * 3. Уровень персонажа
             * 4. Пол участника (м/ж)
             * 5. Ставка за матч в гривнах
             * 
             * Результат заполнения анкеты отобразить в консоли. Отобразить как можно более красиво.
             */
        }
    }
}
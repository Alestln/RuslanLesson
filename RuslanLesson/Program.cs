namespace RuslanLesson
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // int number = int.Parse("123");
            // int number = Convert.ToInt32("123");
            // double number2 = Convert.ToDouble("123.45");
            // char character = Convert.ToChar("A");
            // bool boolean = Convert.ToBoolean("true");

            // int, string, double, char, bool

            /*
             * Целые числа (int, long, short, byte, sbyte, ushort, ulong, uint)
             * 
             * int - целое число
             * Диапазон: от -2,147,483,648 до 2,147,483,647
             * Занимает 4 байта памяти
             * int number = 123;
             * 
             * uint - целое число без знака
             * Диапазон: от 0 до 4,294,967,295
             * Занимает 4 байта памяти
             * 
             * byte - целое число без знака
             * Диапазон: от 0 до 255
             * Занимает 1 байт памяти
             * 
             * sbyte - целое число со знаком
             * Диапазон: от -128 до 127
             * Занимает 1 байт памяти
             * 
             * short - целое число со знаком
             * Диапазон: от -32,768 до 32,767
             * Занимает 2 байта памяти
             * 
             * ushort - целое число без знака
             * Диапазон: от 0 до 65,535
             * Занимает 2 байта памяти
             * 
             * long - целое число со знаком
             * Диапазон: от -9,223,372,036,854,775,808 до 9,223,372,036,854,775,807
             * Занимает 8 байт памяти
             * 
             * ulong - целое число без знака
             * Диапазон: от 0 до 18,446,744,073,709,551,615
             * Занимает 8 байт памяти
             * 
             * Дробные числа (float, double, decimal)
             * 
             * float - число с плавающей точкой одинарной точности (десятичное число)
             * Точность: до 7 знаков после запятой
             * Занимает 4 байта памяти
             * 
             * double - число с плавающей точкой двойной точности (десятичное число)
             * Точность: до 15-16 знаков после запятой
             * Занимает 8 байт памяти
             * 
             * decimal - десятичное число с высокой точностью (обычно используется для финансовых расчетов)
             * Точность: до 28-29 знаков после запятой
             * Занимает 16 байт памяти
             * 
             * string - строка. Строка - это последовательность символов. Писать строку нужно в двойных кавычках.
             * Диапазон: до 2 миллиардов символов (ограничен памятью)
             * Занимает 2 байта на символ. Если строка содержит 100 символов, она займет 200 байт памяти.
             * string name = "Ruslan";
             * 
             * char - одиночный символ. Писать символ нужно в одинарных кавычках.
             * Диапазон: от '\u0000' (0) до '\uffff' (65,535)
             * Занимает 2 байта памяти
             * char letter = 'A';
             * 
             * bool - логический тип данных. Может принимать только два значения: true (истина) или false (ложь).
             * Занимает 1 байт памяти
             * bool isTrue = true;
             */
        }
    }
}